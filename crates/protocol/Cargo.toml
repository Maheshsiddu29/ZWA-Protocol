[package]
name = "zwa-protocol"
description = "Canonical ZWA Protocol types, validation, root envelopes, proof interfaces, and replay state rules"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
thiserror.workspace = true

[lints]
workspace = true
