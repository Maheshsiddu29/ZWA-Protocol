# Circuits

`provenance/` and `eligibility/` contain the Phase 0F/0G circuits migrated without semantic changes. `shared/` contains the frozen JavaScript commitment schemas and the minimal attributed ZK-ORIGIN helpers. Generated witnesses, R1CS, WASM, proving keys, verification keys, and Powers of Tau files must stay outside source control. Phase 0 setups were development-only.
